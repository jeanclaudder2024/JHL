server {
    listen 80;
    server_name justhumanlife.ch www.justhumanlife.ch;

    # Frontend (React App)
    location / {
        root /var/www/jhl/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Backend API Proxy
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Error Logs
    error_log /var/log/nginx/jhl_error.log;
    access_log /var/log/nginx/jhl_access.log;
}
